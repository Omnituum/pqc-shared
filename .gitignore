# ═══════════════════════════════════════════════════════════════════
# GITIGNORE_BASELINE per governance/standards/package-docs/reference/
# Blocks: always node
# Synced via build/GITIGNORE_BASELINE_SWEEP.sh (DO-31)
# ═══════════════════════════════════════════════════════════════════

# ── OS files ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ── Secrets (NEVER commit) ──
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
credentials.json
secrets.json
.secrets/

# ── Local developer overrides ──
.claude/settings.local.json
*.local.json

# ── Dependencies ──
node_modules/
.pnpm-store/
.npm/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ── Build outputs ──
dist/
build/
out/
lib/
*.tsbuildinfo
.tsbuildinfo
next-env.d.ts
.next/

# ── Test / coverage ──
coverage/
.nyc_output/
*.lcov

# ── Monorepo + build caches ──
.turbo/
.cache/
.parcel-cache/

# ── Pack artifacts ──
*.tgz
.packs/

# ═══════════════════════════════════════════════════════════════════
# Repo-specific additions (preserved from prior .gitignore)
# ═══════════════════════════════════════════════════════════════════

# ── Proprietary docs containment ──
# package-docs/ contains canonical-4 + custom-gpt material that is treated
# as proprietary for this public repo. Already-tracked entries are removed
# from the index in the same commit that adds this rule. Structural
# deviation from DOC_STANDARD § Module Locations / § Custom GPT Pattern is
# acknowledged and to be filed against a dedicated DO-## via the Docs
# Organizer Spec GPT (canonical-4 stub-or-references migration + history
# review remain follow-ups outside this commit).
package-docs/

.claude/
data/
*.db
*.db-shm
*.db-wal
.env.local
*.log
npm-debug.log*
pnpm-debug.log*
